{
  "name": "@repo/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "set -a && [ -f ../../.env ] && . ../../.env; set +a && mix phx.server",
    "build": "MIX_ENV=prod mix release",
    "lint": "mix format --check-formatted",
    "type-check": "mix compile --warnings-as-errors",
    "clean": "mix clean",
    "test": "mix test",
    "setup": "mix setup"
  }
}
